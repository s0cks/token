# Dependencies

- Google's Protocol Buffers
- cryptopp
- gRPC

# Building

## Prerequisites

Building will require the dependencies discussed above and CMake

## Compiling

```bash
mkdir build
cd build/
cmake ..
cmake --build .
```