#include "buffer.h"
#include "transaction_signed.h"

namespace token{
  std::string SignedTransaction::ToString() const{
    NOT_IMPLEMENTED(ERROR);
    return "SignedTransaction()";//TODO: implement
  }
}