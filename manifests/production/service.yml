apiVersion: v1
kind: Service
metadata:
  name: token-ledger-svc
  labels:
    app: token
    component: ledger
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    component: ledger