apiVersion: v1
kind: Service
metadata:
  name: token-ledger-svc
  labels:
    app: token
    component: ledger
spec:
  ports:
    - name: rpc
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: LoadBalancer
  selector:
    component: ledger