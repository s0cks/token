apiVersion: v1
kind: Service
metadata:
  name: peers
  labels:
    app: token
    tier: ledger
    component: peers
    environment: prod
spec:
  type: ClusterIP
  selector:
    tier: ledger
    component: peers
    environment: prod
  ports:
    - name: rpc
      port: 8080
      targetPort: 8080
      protocol: TCP