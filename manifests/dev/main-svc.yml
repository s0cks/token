apiVersion: v1
kind: Service
metadata:
  name: main
  namespace: ledger-dev
  labels:
    app: token
    tier: ledger
    component: main
    environment: dev
spec:
  type: ClusterIP
  selector:
    tier: ledger
    component: main
    environment: dev
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: rpc
      port: 8081
      targetPort: 8081
      protocol: TCP